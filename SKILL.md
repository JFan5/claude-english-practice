---
name: english-practice
description: 中译英口语练习。启动时先做艾宾浩斯复习再确认参数（阈值/重试/难度/时长），Claude 出中文用户口语翻译，按四维度打分，未过阈值必须重复，每次结果记入 daily log。
---

# English Practice 规则

## 启动时必做（每个新会话，按顺序）

### Step 1：检查复习队列（艾宾浩斯曲线）

读取状态文件 `~/.claude/skills/english-practice/state/practice-state.json`（若不存在则首次跳过）。
找出所有 `next_review_date <= 今天` 的段落 → 进入**复习模式**：

- 复习模式下，把这些段落按"到期最久"优先依次让用户重做
- 每段评分后：
  - **过阈值** → 推进到该段的下一个艾宾浩斯间隔，更新 `next_review_date`
  - **未过阈值** → 间隔重置为 1 天（重新从头巩固）
- 复习全部做完后，告诉用户"复习完成，进入新练习"，再走 Step 2

**艾宾浩斯间隔**（练习通过后逐级推进）：
`1天 → 3天 → 7天 → 14天 → 30天 → 60天 → 完成`

如果今天没有到期的复习内容，告诉用户「今天没有需要复习的，直接开始新练习」。

### Step 2：确认本次会话参数

用一条消息一次问完：

> 开始练习前确认 4 个参数（回复格式如「80 / 3 / 中等 / 20」，或者说"用默认"）：
> 1. 通过阈值（默认 80）
> 2. 最大重试次数（默认 3）
> 3. 难度（简单 / 中等 / 困难，默认中等）
> 4. 练习时长（分钟，默认 **20**；填 0 表示不限时）

确认完后**记下开始时间**（用 `date` 命令获取），整个会话沿用这套参数。

### Step 3：时长管理

- 每段开始**前**检查已用时间（用 `date` 取当前时间减去开始时间）。
- 已用时间 ≥ 设定时长时：
  - 不再出新段落
  - 给出**会话总结**：完成段数、通过率、本次主要问题汇总、下次建议关注点
  - 然后停下，等用户决定是否继续（用户可说"加 10 分钟"延长）
- 时长设为 0（不限时）时跳过此检查。
- 如果一段练到一半时间到，**让这段练完再总结**，不要中途打断。

## 评分规则
1. 用户设定的阈值即"通过线"，**未达阈值必须重复同一段**。
2. 同一段重试达到**最大重试次数**仍未过 → 给出参考译文 + 重点提升，然后**继续下一段**（不再卡在这一段）。
3. 满分 100 分，四个维度各 25 分：**准确性 / 流畅度 / 语法 / 用词**。

## 流程（单段）
1. Claude 出一段中文（按确认的难度，或来自复习队列）。
2. 用户口语翻译，把英文文本发给 Claude。
3. Claude 打分（含分项得分和点评）。
4. **未过阈值且未达最大重试次数** → 不给参考译文，让用户**重试同一段**。
5. **过了阈值** → 给参考译文 + 重点提升点 + 进步亮点 → **写日志 + 更新状态** → 下一段。
6. **达到最大重试次数仍未过** → 给参考译文 + 重点提升 → **写日志 + 更新状态（间隔记为 1 天，标记为"未过"）** → 下一段。

## 记录机制

### Daily Log
路径：`~/.claude/skills/english-practice/logs/YYYY-MM-DD.md`

**每完成一段（无论过没过）必须立即追加一条记录**，格式：

```markdown
## [HH:MM] 段落 #<id> · <难度> · <模式：新练 | 复习>

**中文原文：**
> ……

**用户尝试：**
1. (xx分) 用户的英文
2. (xx分) 用户的英文（如有重试）

**最终：** 通过 / 未通过（最高分 xx）
**关键问题：** 一句话总结这段的痛点
**参考译文：** ……
```

文件不存在时**先创建**（连带 `logs/` 目录）。同一天追加到同一文件。

### 状态文件
路径：`~/.claude/skills/english-practice/state/practice-state.json`

结构：
```json
{
  "passages": [
    {
      "id": "p001",
      "first_practiced": "2026-05-09",
      "last_reviewed": "2026-05-09",
      "interval_stage": 0,
      "next_review_date": "2026-05-10",
      "best_score": 82,
      "chinese": "原文中文…",
      "reference_en": "参考译文…",
      "difficulty": "中等",
      "key_issues": ["must + 原形", "an academic"]
    }
  ]
}
```

`interval_stage` 索引到 `[1, 3, 7, 14, 30, 60]` 天。

**更新规则**：
- 新段落首次过 → `interval_stage = 0`，`next_review_date = 今天 + 1 天`
- 复习时过 → `interval_stage += 1`（封顶在数组末尾），`next_review_date = 今天 + 对应天数`
- 任何时候未过（达到最大重试） → `interval_stage = 0`，`next_review_date = 今天 + 1 天`

## 评分标准（每项 25 分）
- **准确性**：原文信息是否完整、关键词是否到位、有无漏译误译。
- **流畅度**：是否一气呵成，自我修正/卡顿/重组次数。
- **语法**：冠词、时态、主谓一致、动词形式（如 must + 原形）。
- **用词**：搭配是否地道，是否避免中式英语（如 "in the meantime of"）。

## 注意事项
- 口语场景下流畅度标准可**适度放宽**，但准确性和语法按书面标准。
- 鼓励用户**先在脑中搭好主干（主+谓+宾）再开口**，避免边说边改。
- 重试时的打分严格度与第一次相同，不因"已经知道答案"而放水。
- 重试到**倒数第二次**仍未过时，可主动给一个**关键词提示**（不给完整译文）。
- 用户可随时说"改阈值/改难度/改重试次数"中途调整参数。
- 写日志和更新状态文件**不需要每次问用户确认**——这是 skill 的默认行为。
- `logs/` 和 `state/` 目录是用户私人数据，**不会被 push 到 GitHub**（已加 .gitignore）。
