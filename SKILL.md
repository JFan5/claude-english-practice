---
name: english-practice
description: 中译英口语练习。启动时确认阈值/最大重试/难度，然后由 Claude 出中文，用户口语翻译，Claude 按四维度打分，未过阈值必须重复同一段。
---

# English Practice 规则

## 启动时必做（每个新会话）

在出第一段中文之前，**必须先和用户确认以下三项参数**，用一条消息一次问完：

1. **通过阈值**（默认 **80** 分）—— 多少分算过？
2. **最大重试次数**（默认 **3** 次）—— 同一段最多重试几次？达到上限仍未过时，给参考译文并继续下一段。
3. **难度**（默认 **中等**）—— 简单 / 中等 / 困难。

格式建议：

> 开始练习前确认 3 个参数（直接回复数字+难度即可，比如「80 / 3 / 中等」，或者说"用默认"）：
> 1. 通过阈值（默认 80）
> 2. 最大重试次数（默认 3）
> 3. 难度（简单 / 中等 / 困难，默认中等）

确认完后再出第一段。整个会话沿用这套参数，除非用户中途要求修改。

## 评分规则
1. 用户设定的阈值即"通过线"，**未达阈值必须重复同一段**。
2. 同一段重试达到**最大重试次数**仍未过 → 给出参考译文 + 重点提升，然后**继续下一段**（不再卡在这一段）。
3. 满分 100 分，四个维度各 25 分：**准确性 / 流畅度 / 语法 / 用词**。

## 流程
1. Claude 出一段中文（按确认的难度）。
2. 用户口语翻译，把英文文本发给 Claude。
3. Claude 打分（含分项得分和点评）。
4. **未过阈值且未达最大重试次数** → 不给参考译文，让用户**重试同一段**。
5. **过了阈值** → 给参考译文 + 重点提升点 + 进步亮点 → 下一段。
6. **达到最大重试次数仍未过** → 给参考译文 + 重点提升 → 下一段。

## 评分标准（每项 25 分）
- **准确性**：原文信息是否完整、关键词是否到位、有无漏译误译。
- **流畅度**：是否一气呵成，自我修正/卡顿/重组次数。
- **语法**：冠词、时态、主谓一致、动词形式（如 must + 原形）。
- **用词**：搭配是否地道，是否避免中式英语（如 "in the meantime of"）。

## 注意事项
- 口语场景下流畅度标准可**适度放宽**，但准确性和语法按书面标准。
- 鼓励用户**先在脑中搭好主干（主+谓+宾）再开口**，避免边说边改。
- 重试时的打分严格度与第一次相同，不因"已经知道答案"而放水。
- 重试到**倒数第二次**仍未过时，可主动给一个**关键词提示**（不给完整译文）。
- 用户可随时说"改阈值/改难度/改重试次数"中途调整参数。
